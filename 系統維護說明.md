# 家功場放學趣會計系統維護說明

## 系統定位

- 網站：<https://money.our-reading-space.com/>
- 原始碼：`vickychien1688/money`
- 使用者：負責人與內部會計端；員工在米豆操作，不直接使用本系統。
- 目標：以簡單、自動化的流程完成收入對帳、薪資、特休、支出與營運檢視。
- 個資與薪資金額不寫入本文件；實際資料以網站雲端資料為準。

## 主要功能

### 薪資

- 「本月薪資／特休管理／員工設定」分開處理。
- 薪資明細再分成「正職／英文老師／PT」三個頁籤，一次只顯示一類。
- 正職採員工卡片介面，包含底薪、本月加給、額外課程、遲到、出缺勤與勞健保。
- PT 一般工時依打卡計算，預設時薪由員工名冊設定；可另加家教或小團班課程。
- 英文老師依堂數、堂費、津貼與調整項目計算。
- 完成薪資後寫入申報彙總；申報金額可以手動調整並自動儲存、重算小計。
- 重新執行「確認並完成本月薪資」時，申報金額會依最新薪資試算重新產生。

### 加給

- 每位正職使用「加給項目／明細／金額」表格。
- 固定列包含職務加給、暑假加給與績效獎金，也可新增臨時加給。
- 所有加給自動加總至薪資。
- 暑假加給不是固定每月發放；新月份預設為 0，需要時才填。
- 績效獎金可記錄學期，例如「114（下）」。
- 「其他備註」不計入薪資。

### 打卡與班表

- 支援匯入員工簽到 Excel。
- PT 時數顯示小時與分鐘，可逐日調整。
- 支援手動補登打卡。
- 暑假班表可貼文字或上傳圖片作為核對依據。
- 上傳班表圖片不會直接改薪資數字。

### 特休與保險提醒

- 正職與 PT 均可登記特休。
- 採到職週年制計算，支援匯入米豆簽核完成資料。
- 薪資或固定加給異動時，依 115 年投保級距提醒是否調整。
- 每年八月提醒重新檢視投保級距；116 年級距公布並確認後再更新。
- 系統只做提醒，投保級距由負責人確認後修改。
- 正職新月份的勞保、健保依員工設定中的投保級距與眷屬數自動帶入。
- 舊月份若員工已有投保級距、勞健保卻同時為 0，且未標示為人工調整，視為舊資料漏帶並自動補回。
- 使用者手動修改當月勞保或健保後，系統會記錄為人工值；包含刻意填 0，都不再被自動計算覆蓋。
- 修補舊月份後必須等待雲端同步、重新整理同月份，並確認有投保的正職不再出現勞健保皆為 0。

### 會計師輸出

- 系統內保留完整薪資組成，供負責人核對。
- 「列印給會計師」只輸出員工／項目、申報類別、申報金額、各統編小計與總計。
- 內部加給、課程與備註不出現在會計師精簡表。

### 日記帳與米豆

- 米豆匯出的總收支 Excel 先進核對區，確認後才納入日記帳。
- 長期方向是讓米豆的人事、特休、薪資與會計資料互通，減少重複輸入。
- 匯入時應保留核對步驟，不可讓外部資料直接覆寫已確認的會計資料。

## 資料與安全原則

1. 系統只供負責人及內部會計端使用。
2. 不在 GitHub 文件、程式碼或測試輸出中記錄密碼、員工薪資或其他敏感個資。
3. 員工名冊、薪資、投保級距與當月計算結果以 Supabase 雲端資料為準。
4. 測試正式網站時以唯讀檢查為主，不隨意輸入或變更現有金額。
5. 修改計算規則前，先確認舊月份資料仍能讀取且總額不變。

## 維護與發布流程

1. 在本機專案目錄檢查 `git status`，保留使用者既有變更。
2. 先用 `rg` 找到相關功能，再以小範圍方式修改 `index.html`。
3. 執行 JavaScript 語法檢查與 `git diff --check`。
4. 涉及薪資計算時，另外驗證舊資料搬移前後總額一致。
5. 提交後推送到 GitHub `main`，等待 GitHub Pages 更新。
6. 使用正式網站做唯讀畫面驗證；切換頁籤、檢查文字與總額，不改正式資料。
7. 回報已完成的功能、驗證結果與版本連結。

## 重要檔案

- `index.html`：網站畫面、計算與雲端存取的主要程式。
- `supabase-設定.sql`：會計資料空間與權限設定。
- `README.md`：專案入口與資料位置說明。
- `上線步驟.md`：GitHub Pages、網域與 Supabase 上線設定。
- `系統維護說明.md`：本文件，記錄功能與維護原則。

## Codex 維護 Skill

- Skill 名稱：`maintain-money-accounting`
- 本機位置：`/Users/chienhsiuting/.codex/skills/maintain-money-accounting/SKILL.md`
- 使用方式：在後續工作中指定 `$maintain-money-accounting`，再說明要檢查或修改的會計系統功能。
- Skill 會先讀取本文件，再依安全流程修改、驗證與發布；實際人員和金額仍以雲端資料為準。
- 米豆收款對帳 Skill：`reconcile-mido-payments`
- 本機位置：`/Users/chienhsiuting/.codex/skills/reconcile-mido-payments/SKILL.md`
- 使用方式：指定 `$reconcile-mido-payments`，依序處理銀行 Excel、會計核對、米豆一般繳款、後五碼、發票、會計回填與 LINE 家長通知；禁止使用快速銷帳。

## 完成一項功能的判定

- 畫面符合負責人的實際操作方式。
- 自動儲存正常，重新整理後資料仍存在。
- 小計、總計與申報金額正確。
- 舊月份與既有資料沒有被覆寫。
- 手機與一般電腦寬度均可操作。
- 正式網站已更新並完成唯讀驗證。
