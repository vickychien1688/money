# 薪資單 Email 寄送：一次性設定步驟

網站的「📧 寄薪資單」按鈕要能動，需要完成以下三步（只做一次）。

## 第 1 步：註冊 Resend（Vicky 本人操作）
1. 到 https://resend.com 用 vickychien127@gmail.com 註冊（免費方案每天 100 封、每月 3,000 封，夠用）。
2. 登入後到「Domains」→「Add Domain」→ 輸入 `our-reading-space.com`。
3. Resend 會列出幾筆 DNS 紀錄（MX、TXT/SPF、DKIM），先留在畫面上。

## 第 2 步：到 PChome 買網址加 DNS 紀錄
1. 登入 PChome 買網址 → our-reading-space.com 的 DNS 設定（路徑見記憶 domain-dns-pchome）。
2. 把 Resend 列的每一筆照抄新增（主機名稱、類型、內容都要一樣）。
3. 回 Resend 按「Verify」，等它全部變綠色（有時要等幾十分鐘）。
4. 驗證通過後到「API Keys」→「Create API Key」（權限 Sending access 就好），複製金鑰。

## 第 3 步：部署 Supabase 寄信程式
1. 登入 https://supabase.com/dashboard → 專案 nvnrkzveyehcnlaszzin。
2. Edge Functions → Deploy a new function → 名稱填 `send-payslip`，程式碼貼 `supabase/functions/send-payslip/index.ts` 的內容。
3. Edge Functions → Secrets 新增：
   - `RESEND_API_KEY`＝第 2 步複製的金鑰
   - `MAIL_FROM`＝`家功場放學趣 <payroll@our-reading-space.com>`（可改顯示名稱）

## 完成後怎麼用（每月流程）
1. 「員工設定」確認每位老師的 Email 欄有填（可用「整批貼上老師 Email」從 Excel 貼）。
2. 本月薪資確認完 → 薪資單卡片按「📧 寄測試信給自己」→ 去自己信箱看版面。
3. 版面 OK → 按「📧 寄薪資單到老師信箱」→ 畫面會列出每個人要寄到哪裡，確認後送出。
4. 系統會記錄本月已寄過（再按會提醒，避免重複寄）。

## 安全設計
- 只有會計系統的四個授權帳號（登入後）能呼叫寄信後台，名單寫在 `supabase/functions/send-payslip/index.ts` 的 `ALLOWED`。
- Resend 金鑰只存在 Supabase Secrets，不在網站程式碼裡。
