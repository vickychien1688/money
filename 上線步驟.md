# money.our-reading-space.com 上線步驟

網頁本體已完成（index.html），照下面四步就能上線。每一步 Claude 都可以陪妳做。

## 第 1 步：建 GitHub repo 並推上去
1. GitHub Desktop → Add Existing Repository → 選 `Downloads/這邊的東西不能刪/money-tool`
   （若說不是 repo，就選 create a repository）
2. Publish repository，名稱建議 `money`，帳號 vickychien1688，**不要勾 private**（GitHub Pages 免費版要 public；網頁本身有登入保護，帳沒登入看不到）
3. 推上去（Push origin）

## 第 2 步：開 GitHub Pages
1. github.com/vickychien1688/money → Settings → Pages
2. Source 選 `main` branch、`/ (root)` → Save
3. Custom domain 填 `money.our-reading-space.com` → Save（CNAME 檔已備好）
4. 等一下後勾 Enforce HTTPS

## 第 3 步：PChome DNS 加一筆
到 PChome 買網址的 DNS 管理（跟之前加 ws 子網域同一個地方）：
- 類型：CNAME
- 主機名稱：`money`
- 指向：`vickychien1688.github.io`

## 第 4 步：Supabase 開會計專用空間
1. supabase.com 登入 → 專案（nvnrkzveyehcnlaszzin，跟 ws 工具同一個）
2. SQL Editor → 貼上整份 `supabase-設定.sql` → Run
3. 目前有權限的帳號：vickychien127@gmail.com（老闆）、a-chuen@yahoo.com.tw（會計，2026-08-19 加回）。其他帳號即使仍供米豆或其他系統使用，也不授予 accounting 空間權限

## 完成後
- Vicky：用 vickychien127@gmail.com 的 Supabase 帳密登入（跟 ws 工具同一組）
- 資料存在 Supabase `accounting` 空間，跟跟讀平台、ws 工具互不影響
